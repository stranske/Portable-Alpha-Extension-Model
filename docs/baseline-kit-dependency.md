# app-baseline-kit dependency

Portable-Alpha-Extension-Model uses **Pattern A**. `app-baseline-kit` is declared unpinned `@main` in `pyproject.toml` (line ~134) and is absent from `requirements.lock`. Note: an earlier revision SHA-pinned this dependency, but commit 9159ceb **reverted it back to `@main`** because lock validation was incorrectly flagging the URL dep as missing (PEP 508 direct references carry no `==`, so they are URL-pinned, not version-pinned, and are correctly omitted from the lock version check). At test time the package is resolved from `@main` by `pip install -e .` via the reusable `reusable-10-ci-python.yml`. Because the pyproject reference is already unpinned `@main`, this repo is Pattern A, not the deprecated frozen-SHA Pattern C — no item-2 change is required. (Recommended follow-up only: add `[tool.uv.pip] no-emit-package` to match canonical Pattern A and forestall any future lock-SHA conflict, and move to a tagged ref once the package is versioned.)
